		Human Resources: A Learning Project

This is a self learning project names as HR: Human Resources.
There are different kind of designation, employee, department are added later with the time, which will be introduces as a features. 
This project is Three Layed programming project, which are Data Layer, Business Layer, and Presentation Layer. We worked on each layer one by one.
Though based on commits, you can learn about the project in step wise.
At very first, we have created a structure of project we want to folders that are used to classified each level of programs.
--------------------------------------------------------------------------------
1: We need a exception classes to handle all type of problem arrived while coding.
		***Exception class: DAOException***
For each layer, their are unique and different exception classes for them, for data layer, we are created a exception class on path: 'hr/dl/src/com/thinking/machines/hr/dl/exceptions'.
--------------------------------------------------------------------------------
Now, Start creating project: 
2: First we are created an interface for Designation Data Transfer Object class of Data Layer, which is called DesignationDTOInterface. At the very first learning stage names are like this such that its memories as DTO and DAO.
DTO: Data Transfer Object		: Used to share data to layers
There are total of two property/field added in Designation class which are (a) code[int] and (b) title[String], for designation name and designation code.
After creating interface, we are creating its implementation on path: 'hr/dl/src/com/thinking/machines/hr/dl/dto'.
We have created a tmp folder for doing all unnecessary works. We are generating Setter Geeter from our tool for DesignationDTO class.
For DesignationDTO, We are created setter getter and necessary functionality like hashCode, equals and compareTo. Those are required functionality for any DTO class.
--------------------------------------------------------------------------------
3: Creating interface for Designation Data Access Object Class of Data Layer, which is called DesignationDAOInterface.
DAO: Data Access Object			:Used to access data from layers
There are CRUD operation performed on Designation Data's, which would be handle by this class. All the necessary method which required to perform operations are declared in this interface.
After declaring, It should be defined in seperate dao folder class names as DesignationDAO.
There are many methods like add, update, delete, getAll, getByCode, getByTitle, isCodeExist, isTitleExist, and getCount. All of this methods are available but thier implementation need to be checked after each steps, so we have declared them as empty with a exception 'throw new DAOException("Not yer implemented");' such that there are no need for returning any values.
On each implementation, we have to create thier testcases and try that our methds are written well or not.
One more thing that all of this methods are written with throws DAOException as declared, which means they all methods are using checked exception. On using all of this methods, Used must have to put a try and catch block to handle exceptions.
--------------------------------------------------------------------------------
Learning: First all data related data layer is managed by file handling, then changed it to using RBDMS: Database management (MySQL).
3-1: void add(DesignationDTOInterface) throws DAOException;
There are some validation needs to performed and implementation done as per below 
	a) DesignationDTOInterface should not be null.
	b) Title are seperate out from DesignationDTOInterface. 
	c) Code used is unique, and maintained as Primary Key. Generate code and set in DesignationDTOInterface, Then user can get designation code from there. We want unique code, user may blundered here, so we are using this technique.
	d) Title need to be unique, We are travers on all records and not to add title if its already available. Data redundancy checked.
	e) Generated code and title is set in file [ Data structure maintained(optional)].
	f) Header added at first before add records in file. 10 space for lastGeneratedCode and 10 space for totalNumberOfRecords.
	We have to manually create 10 space area for that, trim that string before converting it to Integer. 
3-2: void remove(int code) throws DAOException;
There are data removed from file if exist but before that we have to checks availibility of the code, then take all data in a tmp file except that code data which has to remove then shift all data from tmp file to our data file. 
3-3: void update(DesignationDTOInterface) throws DAOException;
There are data to update based on code as primary key. First we have to checks availibility of code and then we have to check availibility of the designation title against other code. if title already exist against another code then Do not update data. Over here, We have to change recordCount to -1 and then change in file manually.
If all conditions are satisfied and there are no more errors and exceptions then==> create a tmp file to store all other code data and add that code data with updated values, then shift all data from tmpfile to main file.
3-4: Code exist, title exist , getByCode, getByTitle are all methods in which we have to check and if found data then return required things. There are no need to change recordsCount and lastGeneratedCode whatever those things. Just find and return.
3-5: Set<DesignationDTOInterface> getAll() throw DAOException;
     We have to create a TreeSet and return after fill all data from file. There are Set Returns which is Super class of TreeSet, So Data can be transformed from Set to whatever Class (HashSet,HashLinkedSet,TreeSet). User can do as per basis of thier requirement.
================================================================================
	
			Employee DTO/DAO Class Implementation

We are creating a class for Employee Entry Management, There are following fields are available. 
1: String 	employeeId		Primary Key, Unique, Auto Generated
2: String 	name
3: int 		designationCode
4: Date 	dateOfBirth				(java.util.*)
5: char 	gender
6: boolean 	isIndian
7: BigDecimal	basicSalary				(java.math.*)
8: String 	panNumber		Unique
9: String 	aadharCardNumber	Unique
------------
EmployeeId need to be primary Key and auto generated, Example: 
employeeId: A100000000001 is first employee id
then 10th:  employee id is A100000000010
100th : A100000000100
Total Length of one id: 13: 1->character, 12->integer
Max Generated Code: A999999999999, MAX number of records
------------
Over here, DTO Class implemented with all this fields. 
Now, Implementation of thier functionalities in DAO Class.

Employee DAO Class: 
There are lots of methods involved for managing Employee data, We are start implemetation from here.
DesignationCode used to manage Designation of each employee and for use Designation based data, We are using GetByDesignationCode(designationCode) in which returning Set<> will give provide complete date. Also, We can check existance of Designation Code using isDesignationAlloted(designationCode) methods.
We are checking existance of aadharCardNumber, panNumber, employeeId and seperately get Data based on thier seperate provided id or numbers.
===============================================================================
For the validation of Gender field, which is fixed so we have to use enumeration(enum). Enumeration is a way in which we can create a Data Type which contains some values, and we can take any value against that data type from those values only. Only that values are assigned towards that Data Type.
Example: 
public enum GENDER{MALE,FEMALE}
Data Type: GENDER
Values: MALE,FEMALE		Only values can be assigned towards GENDER otherwise code not compiled.
We have to manage the same for every layer: DL,BL,PL
That's Why we are creating those enum things in common area of HR project.
Those Things are also used for testcases too.
===============================================================================

